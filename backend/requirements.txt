# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Templates
jinja2==3.1.4

# HTTP and config
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0

# PDF rendering (run `playwright install chromium` once after installing)
playwright==1.48.0

# Emoji parsing for the stats / wrapped engines
emoji==2.14.0

# NOTE: The LLM client (app/llm.py) talks to Groq and Ollama directly over
# HTTP via httpx, so no SDK packages are required. pandas/numpy/python-magic
# were previously listed but are never imported anywhere in the app — they
# were removed to keep installs fast and avoid the python-magic-bin /
# numpy-build failures common on Windows + Python 3.13.
